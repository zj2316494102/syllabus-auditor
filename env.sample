# syllabus-auditor 环境变量示例
# 复制为 .env 后填写实际值：cp env.sample .env  （Windows: copy env.sample .env）

# -----------------------------------------------------------------------------
# PostgreSQL（必需）
# -----------------------------------------------------------------------------
POSTGRES_ADDRESS=localhost:5432
POSTGRES_DB=course
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password

# -----------------------------------------------------------------------------
# LLM（direct_llm / rag_llm 审核维度需要，按 project.yaml 中配置的 provider 填写）
# -----------------------------------------------------------------------------
OPENAI_API_KEY=

# OpenAI 兼容接口（如 Qwen、DeepSeek 等）
LLM_BASE_URL=
LLM_API_KEY=EMPTY
LLM_MODEL_NAME=

# -----------------------------------------------------------------------------
# Embedding（仅 rag_llm 检索需要；direct_llm 与纯 rule 可不填）
# -----------------------------------------------------------------------------
EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=EMPTY
EMBEDDING_MODEL_NAME=BAAI/bge-m3
EMBEDDING_DIM=1024

# -----------------------------------------------------------------------------
# 可选：应用行为
# -----------------------------------------------------------------------------
# LOG_LEVEL=INFO
